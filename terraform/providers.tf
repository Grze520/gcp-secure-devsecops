terraform {
  required_version = ">= 1.5.0"
  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
  }
}

provider "google" {
  # Dane projektu i tokeny OIDC zostaną wstrzyknięte automatycznie przez GitHub Actions
}